<UserControl x:Class="Messenger_Project.SearchResultsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <StackPanel Margin="20">

        <Button Content="Close Search" Width="100" HorizontalAlignment="Left" Margin="0,0,0,20" Click="Close_Click"/>

        <TextBlock Text="Search Results:" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <ListBox x:Name="ResultsListBox" BorderThickness="0" FontSize="16">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Username}" Margin="5"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </StackPanel>
</UserControl>